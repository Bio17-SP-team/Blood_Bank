#include "Recipient .h"+
 #include<iostream>
 #include"stdio.h"
 using namespace std ;
void availabilityStatus() {
  cout<< " The required blood type and quantity are Available"
}
bool Blood_Bank::searchForAvalabilityOfBloodTypes(int quantity, string blood_type)
{
    
    // badawar 3ala el A type
    if (blood_type == "A+"){
    if (blood_type == "A+"&& Aplus.size()>=quantity-1)
    {
      return true ;
      availabilityStatus();
      cout <<" A type is avalible ";
    }


    else if (blood_type == "B+" && Bplus.size() >= quantity - 1)
    {      return true ;
        availabilityStatus();
        cout <<" B type is avalible ";
  
    else if (blood_type == "o+" && oplus.size() >= quantity - 1)
     {      return true ;
        availabilityStatus();
        cout <<" o type is avalible ";
      }
    else
    {
        cout << "Your request is not Available "
        cout<< " you are directed to the waiting list"
          return false;
    }
    }
    // badawar 3ala el b
    if (blood_type == "B+" ){

  if (blood_type == "B+" && Bplus.size() >= quantity - 1)
    {
       return true ;
      availabilityStatus();
      cout <<" B type is avalible ";

    }
    else  if (blood_type == "o+"&& Oplus.size()>=quantity-1)
    {
         return true ;
      availabilityStatus();
      cout <<" O type is avalible ";
    }
    else{
    cout << "Your request is not Available "
    cout<< " you are directed to the waiting list"
      return false;
    }
    }


// o type
  if (blood_type == "O+"){
   if (blood_type == "O+" && Oplus.size() >= quantity - 1)
    {

      return true ;
   availabilityStatus();
   cout <<" O type is avalible ";
   }
   else{
   cout << "Your request is not Available "
   cout<< " you are directed to the waiting list"
     return false;
   }
 }
 
   
  if (blood_type == "AB+" && ABplus.size() >= quantity - 1)
      {

        return true ;
        availabilityStatus();
        cout <<" AB type is avalible ";
      }
      else if (blood_type == "O+" && Oplus.size() >= quantity - 1)
       {

         return true ;
      availabilityStatus();
      cout <<" O type is avalible ";
      }
    else  if (blood_type == "A+"&& Aplus.size()>=quantity-1)
      {
        return true ;
        availabilityStatus();
        cout <<" A type is avalible ";
      }
    else
    {
      cout << "Your request is not Available "
      cout<< " you are directed to the waiting list"
      return false;
    }
